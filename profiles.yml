jaffle_shop:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      keyfile: dbt-testing-creds.json
      project: dbt-testing-332412
      dataset: dbt_bjorn # You can also use "schema" here
      threads: 1
      timeout_seconds: 300
      location: europe-west4 # Optional, one of US or EU
      priority: interactive
      retries: 1
